<!-- Generated by `npm run build:context` (GRAMMAR.md §10). Do not edit by hand. -->

# <zbk-checkbox>

The zebkit checkbox: a light-DOM element wrapping a real
`<input type="checkbox">` inside its own `<label>`. The invisible native
input stretches across the whole label (hover, press, click, and focus land
on it natively anywhere); an aria-hidden control span visualizes its state.
No custom events — the native `change`/`input` bubble.

Base class: `.zbk-checkbox`. Variant classes: `.zbk-checkbox--{variant}`. Tokens: `--zbk-checkbox-{property}[-{state}]`. Write `aria-*`/`role` on the element; they relocate to the internal native element. `focus()` forwards to the internal focusable.

## When to use

- An independent on/off choice submitted with a form ('I agree to the terms').
- Selecting any number of items from a list — each checkbox stands alone.
- A parent summarizing a group: `indeterminate` expresses 'some but not all'.
- NOT Exactly one choice among mutually exclusive options. → zbk-radio
- NOT The setting takes effect immediately, with no form submit. → zbk-toggle

## Guidance

- `indeterminate` is visual and AT state only — it is never submitted, and any user toggle clears it (native behavior). Set it from the group's state, don't ask users to.
- The invisible native input stretches across the whole label: hover, press, click, and focus land on it natively anywhere — never add click handlers to fake this.
- Use `toggles` to bind checked to a retained region's visibility; `shows` and `hides` fire only when the checkbox enters checked.

## Attributes

| Attribute | Type | Default | Description |
|---|---|---|---|
| `checked` | `boolean` | `false` | Whether the checkbox is checked. Syncs from user interaction. |
| `indeterminate` | `boolean` | `false` | The mixed "some but not all" state. Visual + AT state only, never submitted; any user toggle clears it (native behavior). |
| `disabled` | `boolean` | `false` | Native disabled: removed from the tab order, blocks all interaction. |
| `required` | `boolean` | `false` | Forwarded for native constraint validation. |
| `name` | `string \| undefined` | — | Forwarded to the internal input for native form participation. |
| `value` | `string` | `'on'` | The submitted value; defaults to the platform's "on". |
| `toggles` | `string \| undefined` | — | Bind checked to the visibility of every space-separated target id. |
| `shows` | `string \| undefined` | — | Show every target when this checkbox enters the checked state. |
| `hides` | `string \| undefined` | — | Hide every target when this checkbox enters the checked state. |
| `discover-toggled-element-timeout` | `number \| undefined` | — | Per-instance late-target discovery window in milliseconds; 0 diagnoses immediately. |
| `variant` | `string` | `""` | Space-separated registered variant names, e.g. "ghost lg". Unknown names warn with the registered vocabulary. |

## Slots

| Slot | Description | Notes |
|---|---|---|
| *(default)* | The label content (the accessible name via the wrapping label). | required |
| `checked` | Indicator content shown while checked; replaces the drawn checkmark. Any markup: svg, icon-font glyph, HTML character, image. | aria-hidden; sized by `indicator-size`; colored by `indicator-ink` |
| `unchecked` | Indicator content shown while neither checked nor indeterminate. | aria-hidden; sized by `indicator-size`; colored by `indicator-ink` |
| `indeterminate` | Indicator content shown while indeterminate; replaces the drawn bar. | aria-hidden; sized by `indicator-size`; colored by `indicator-ink` |

- `checked`: The control is aria-hidden — the state is conveyed by the native input, so indicator content is purely visual.
- `indeterminate`: Each state slot only affects its own state — a customized checkmark leaves the drawn indeterminate bar in place unless this slot is also filled.

## Keyboard

- `Space` — Toggles checked (native input behavior, preserved).

## Options (behavior)

Named, defaulted decisions about what this component conveniently *does*, set under `components.checkbox.options` in zebkit.config.json and carried into the browser by the generated runtime config module. `false` is the off switch for any of them. Precedence is one chain with no exceptions: **instance attribute > project config > zebkit default** — whatever the config decided, the markup outranks it.

| Option | Type | Default | Does |
|---|---|---|---|
| `discoverToggledElementTimeout` | `number` | `2000` | How long to watch for a visibility target that mounts after its trigger, in milliseconds; false or an instance value of 0 diagnoses immediately. |

## Composites

Visibility participant: **trigger** (binding). Author one of `toggles="{id ...}"`, `shows="{id ...}"`, `hides="{id ...}"`. It may emit `aria-controls` according to the target mechanism. See [Visibility composites](composites.md) for target resolution, events, late mounting, and degradation.

## Events

No custom events. Native events (`click`, `change`, `input`, ...) bubble from the internal native element in light DOM — listen on the zebkit element.

## Examples

**Basic**

```html
<zbk-checkbox name="terms" required>I agree to the terms</zbk-checkbox>
```

**Custom indicators**

```html
<zbk-checkbox name="favorite" checked>Add to favorites<span slot="checked">★</span><span slot="unchecked">☆</span></zbk-checkbox>
```

Indicator content layers over the aria-hidden control, sized by indicator-size and colored by indicator-ink; the drawn checkmark is replaced only for the states provided.

**Indeterminate group parent**

```html
<zbk-checkbox indeterminate>Select all</zbk-checkbox>
```

Set indeterminate from the child checkboxes' mixed state; toggling the parent clears it natively.

**Bound details**

```html
<zbk-checkbox toggles="terms-details">Show terms</zbk-checkbox><section id="terms-details" hidden="until-found">Terms and conditions.</section>
```

Checked is the binding source: selecting the checkbox reveals the retained region, and another bound trigger closing it unchecks the checkbox.

**Labelless checkbox** (discouraged)

```html
<zbk-checkbox name="row-1"></zbk-checkbox>
```

No accessible name. Even in dense table rows, give it label children (visually hidden if needed) or aria-label naming what it selects.

## Tokens (CSS custom properties)

Values are alias references (`{family.name}` compiles to `var(--zbk-family-name)`) or structural literals. Override any of them on a selector to re-style; nothing else is needed.

| Token | Default | Type | Condition | Description |
|---|---|---|---|---|
| `--zbk-checkbox-control-width` | `{spacing.1}` | dimension | — | Width of the checkbox control. |
| `--zbk-checkbox-control-height` | `{spacing.1}` | dimension | — | Height of the checkbox control. |
| `--zbk-checkbox-canvas` | `{app.canvas}` | color | — | Background of an unchecked checkbox control. |
| `--zbk-checkbox-canvas-hover` | `{app.canvas-subtle}` | color | — | Control background when hovered. |
| `--zbk-checkbox-canvas-active` | `{app.canvas-muted}` | color | — | Control background while pressed. |
| `--zbk-checkbox-canvas-checked` | `{action.ink}` | color | — | Control background when checked. |
| `--zbk-checkbox-canvas-checked-hover` | `{action.ink}` | color | — | Control background when checked and hovered. |
| `--zbk-checkbox-canvas-checked-active` | `{action.ink}` | color | — | Control background when checked and pressed. |
| `--zbk-checkbox-canvas-checked-disabled` | `{checkbox.canvas-disabled}` | color | — | Control background when checked and disabled. |
| `--zbk-checkbox-canvas-indeterminate` | `{action.ink}` | color | — | Control background in the indeterminate state. |
| `--zbk-checkbox-canvas-indeterminate-hover` | `{action.ink}` | color | — | Control background when indeterminate and hovered. |
| `--zbk-checkbox-canvas-indeterminate-active` | `{action.ink}` | color | — | Control background when indeterminate and pressed. |
| `--zbk-checkbox-canvas-indeterminate-disabled` | `{checkbox.canvas-disabled}` | color | — | Control background when indeterminate and disabled. |
| `--zbk-checkbox-canvas-disabled` | `{disabled.canvas}` | color | — | Control background when disabled. |
| `--zbk-checkbox-border-color` | `{app.border-emphasis}` | color | — | Border color of an unchecked checkbox control. |
| `--zbk-checkbox-border-color-hover` | `{action.ink}` | color | — | Control border color when hovered. |
| `--zbk-checkbox-border-color-active` | `{action.ink}` | color | — | Control border color while pressed. |
| `--zbk-checkbox-border-color-checked` | `{action.ink}` | color | — | Control border color when checked. |
| `--zbk-checkbox-border-color-checked-hover` | `{action.ink}` | color | — | Control border color when checked and hovered. |
| `--zbk-checkbox-border-color-checked-active` | `{action.ink}` | color | — | Control border color when checked and pressed. |
| `--zbk-checkbox-border-color-checked-disabled` | `{checkbox.border-color-disabled}` | color | — | Control border color when checked and disabled. |
| `--zbk-checkbox-border-color-indeterminate` | `{action.ink}` | color | — | Control border color in the indeterminate state. |
| `--zbk-checkbox-border-color-indeterminate-hover` | `{action.ink}` | color | — | Control border color when indeterminate and hovered. |
| `--zbk-checkbox-border-color-indeterminate-active` | `{action.ink}` | color | — | Control border color when indeterminate and pressed. |
| `--zbk-checkbox-border-color-indeterminate-disabled` | `{checkbox.border-color-disabled}` | color | — | Control border color when indeterminate and disabled. |
| `--zbk-checkbox-border-color-disabled` | `{disabled.border}` | color | — | Control border color when disabled. |
| `--zbk-checkbox-border-width` | `{border.width-sm}` | dimension | — | Border thickness of the checkbox control. |
| `--zbk-checkbox-border-style` | `{border.style}` | strokeStyle | — | Border style of the checkbox control. |
| `--zbk-checkbox-border-radius` | `{border.radius-xs}` | dimension | — | Corner radius of the checkbox control. |
| `--zbk-checkbox-indicator-ink` | `{app.ink-inverse}` | color | — | Color of the checkmark and indeterminate bar. |
| `--zbk-checkbox-indicator-ink-checked` | `{checkbox.indicator-ink}` | color | — | Checkmark color when checked. |
| `--zbk-checkbox-indicator-ink-checked-hover` | `{checkbox.indicator-ink-checked}` | color | — | Checkmark color when checked and hovered. |
| `--zbk-checkbox-indicator-ink-checked-active` | `{checkbox.indicator-ink-checked}` | color | — | Checkmark color when checked and pressed. |
| `--zbk-checkbox-indicator-ink-checked-disabled` | `{checkbox.indicator-ink-disabled}` | color | — | Checkmark color when checked and disabled. |
| `--zbk-checkbox-indicator-ink-indeterminate` | `{checkbox.indicator-ink}` | color | — | Indicator bar color in the indeterminate state. |
| `--zbk-checkbox-indicator-ink-indeterminate-hover` | `{checkbox.indicator-ink-indeterminate}` | color | — | Indicator bar color when indeterminate and hovered. |
| `--zbk-checkbox-indicator-ink-indeterminate-active` | `{checkbox.indicator-ink-indeterminate}` | color | — | Indicator bar color when indeterminate and pressed. |
| `--zbk-checkbox-indicator-ink-indeterminate-disabled` | `{checkbox.indicator-ink-disabled}` | color | — | Indicator bar color when indeterminate and disabled. |
| `--zbk-checkbox-indicator-ink-disabled` | `{disabled.ink}` | color | — | Indicator color when disabled. |
| `--zbk-checkbox-indicator-stroke-width` | `{border.width-sm}` | dimension | — | Stroke width of the checkmark; also the indeterminate bar's thickness. |
| `--zbk-checkbox-indicator-radius` | `{border.radius-xs}` | dimension | — | Corner radius of the indeterminate bar. |
| `--zbk-checkbox-indicator-size` | `{spacing.1}` | cssDimension | — | Size of slotted indicator content (checked/unchecked/indeterminate slots); 1em tracks the component's font-size so size variants rescale it. |
| `--zbk-checkbox-ink` | `{app.ink}` | color | — | Label text color. |
| `--zbk-checkbox-ink-disabled` | `{disabled.ink}` | color | — | Label text color when disabled. |
| `--zbk-checkbox-font-family` | `{font-family.interface}` | fontFamily | — | Font family for the checkbox label. |
| `--zbk-checkbox-font-size` | `{p.font-size}` | cssDimension | — | Font size for the checkbox label. |
| `--zbk-checkbox-font-weight` | `{font-weight.normal}` | fontWeight | — | Font weight for the checkbox label. |
| `--zbk-checkbox-line-height` | `{line-height.2}` | number | — | Line height for the checkbox label. |
| `--zbk-checkbox-letter-spacing` | `{letter-spacing.normal}` | cssDimension | — | Letter spacing for the checkbox label. |
| `--zbk-checkbox-gap` | `{spacing.sm}` | dimension | — | Space between the control and its label. |
| `--zbk-checkbox-align-items` | `center` | flex | — | Cross-axis alignment of control and label. |
| `--zbk-checkbox-group-gap` | `{spacing.md}` | dimension | — | Gap between checkboxes in a .zbk-checkbox-group. |
| `--zbk-checkbox-group-direction` | `column` | flex | — | Flow direction of a .zbk-checkbox-group (column or row). |
| `--zbk-checkbox-focus-color` | `{focus.color}` | color | — | Outline color for keyboard focus. |
| `--zbk-checkbox-focus-width` | `{focus.width}` | dimension | — | Outline width for keyboard focus. |
| `--zbk-checkbox-focus-offset` | `{focus.offset}` | dimension | — | Outline offset for keyboard focus. |
| `--zbk-checkbox-box-shadow` | `none` | shadow | — | Default control shadow. |
| `--zbk-checkbox-box-shadow-hover` | `none` | shadow | — | Control shadow when hovered. |
| `--zbk-checkbox-box-shadow-active` | `none` | shadow | — | Control shadow while pressed. |
| `--zbk-checkbox-box-shadow-checked` | `none` | shadow | — | Control shadow when checked. |
| `--zbk-checkbox-box-shadow-checked-hover` | `{checkbox.box-shadow-checked}` | shadow | — | Control shadow when checked and hovered. |
| `--zbk-checkbox-box-shadow-checked-active` | `{checkbox.box-shadow-checked}` | shadow | — | Control shadow when checked and pressed. |
| `--zbk-checkbox-box-shadow-checked-disabled` | `none` | shadow | — | Control shadow when checked and disabled. |
| `--zbk-checkbox-box-shadow-indeterminate` | `{checkbox.box-shadow-checked}` | shadow | — | Control shadow in the indeterminate state. |
| `--zbk-checkbox-box-shadow-indeterminate-hover` | `{checkbox.box-shadow-indeterminate}` | shadow | — | Control shadow when indeterminate and hovered. |
| `--zbk-checkbox-box-shadow-indeterminate-active` | `{checkbox.box-shadow-indeterminate}` | shadow | — | Control shadow when indeterminate and pressed. |
| `--zbk-checkbox-box-shadow-indeterminate-disabled` | `none` | shadow | — | Control shadow when indeterminate and disabled. |
| `--zbk-checkbox-box-shadow-focus` | `none` | shadow | — | Control shadow in the focus state (in addition to the outline). |
| `--zbk-checkbox-box-shadow-disabled` | `none` | shadow | — | Control shadow when disabled. |
| `--zbk-checkbox-cursor` | `pointer` | cursor | — | Cursor when hovering an enabled checkbox. |
| `--zbk-checkbox-cursor-disabled` | `not-allowed` | cursor | — | Cursor when hovering a disabled checkbox. |
| `--zbk-checkbox-transition-duration` | `{transition.playful-fx-duration-default}` | duration | — | Duration for checkbox state transitions. **(a11y)** |
| `--zbk-checkbox-transition-timing-function` | `{transition.playful-fx-function-default}` | cubicBezier | — | Easing for checkbox state transitions. |
| `--zbk-checkbox-transition-property` | `background-color, border-color, box-shadow, transform, opacity, outline` | transitionProperty | — | CSS properties that animate on state changes. |
| `--zbk-checkbox-transition-delay` | `0ms` | duration | — | Delay before checkbox transitions run. |
| `--zbk-checkbox-opacity` | `1` | number | — | Opacity of the checkbox. |
| `--zbk-checkbox-opacity-disabled` | `{opacity.70}` | number | — | Control opacity when disabled. |

## Variants

A variant is a named, partial remapping of the token surface compiled to a class. Apply via `variant="name other-name"`; different axes compose, same-axis variants are alternatives.

| Variant | Axis | Class | Description | Overrides |
|---|---|---|---|---|
| `lg` | size | `zbk-checkbox--lg` | Larger control and label type. | control-width: {spacing.lg}; control-height: {spacing.lg}; font-size: {font-size.lg} |
| `sm` | size | `zbk-checkbox--sm` | Smaller control and label type. | control-width: {spacing.md}; control-height: {spacing.md}; font-size: {font-size.sm} |

Custom variants: add a `zbk-checkbox.variants.json` file to the base theme's token folder (component-keyed map of `{ "checkbox": { "{name}": { "overrides": { ... } } } }`; token keys must exist in the table above, values are alias references or structural literals). A shipped variant name patches that variant's CSS — usable immediately. A new name compiles a new `.zbk-checkbox--{name}` class and additionally needs `ZebkitElement.registerVariants(json)` before elements upgrade so `variant="{name}"` validates and applies it.

Related: `<zbk-radio>`, `<zbk-toggle>`.

## Resolved options in this project

What `components.checkbox.options` resolves to here, carried into the browser by the generated runtime config module. An instance attribute always outranks these (GRAMMAR.md §11).

| Option | Value |
|---|---|
| `discoverToggledElementTimeout` | `2000` |
