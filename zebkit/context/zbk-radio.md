<!-- Generated by `npm run build:context` (GRAMMAR.md §10). Do not edit by hand. -->

# <zbk-radio>

The zebkit radio: a light-DOM element wrapping a real
`<input type="radio">` inside its own `<label>`. Radios sharing a `name`
form a native group — mutual exclusion, arrow-key navigation, and form
submission come from the platform. No custom events — the native `change`
bubbles from the newly selected input.

Base class: `.zbk-radio`. Variant classes: `.zbk-radio--{variant}`. Tokens: `--zbk-radio-{property}[-{state}]`. Write `aria-*`/`role` on the element; they relocate to the internal native element. `focus()` forwards to the internal focusable.

## When to use

- Exactly one choice among a small set of mutually exclusive options, all visible at once.
- The choice is submitted with a form rather than taking effect immediately.
- NOT An independent on/off choice. → zbk-checkbox
- NOT The setting takes effect immediately, with no form submit. → zbk-toggle
- NOT The option list is long or space is constrained. → zbk-select

## Guidance

- Radios only behave as one group when they share a `name` — the group, not each radio, is the single form control.
- A group should have a group label (a <fieldset>/<legend> or aria-labelledby on a wrapping group) in addition to each radio's own label.
- Use `toggles` on each radio to bind mutually exclusive panels; `shows` alone accumulates visible panels because leaving checked performs no action.

## Attributes

| Attribute | Type | Default | Description |
|---|---|---|---|
| `checked` | `boolean` | `false` | Whether this radio is the group's selection. Syncs from user interaction. |
| `disabled` | `boolean` | `false` | Native disabled: removed from the tab order, blocks all interaction. |
| `required` | `boolean` | `false` | Forwarded for native constraint validation. |
| `name` | `string \| undefined` | — | The group key. Radios only behave as one group when they share a name. |
| `value` | `string` | `'on'` | This option's submitted value; defaults to the platform's "on". |
| `toggles` | `string \| undefined` | — | Bind checked to the visibility of every space-separated target id. |
| `shows` | `string \| undefined` | — | Show every target when this radio enters the checked state. |
| `hides` | `string \| undefined` | — | Hide every target when this radio enters the checked state. |
| `discover-toggled-element-timeout` | `number \| undefined` | — | Per-instance late-target discovery window in milliseconds; 0 diagnoses immediately. |
| `variant` | `string` | `""` | Space-separated registered variant names, e.g. "ghost lg". Unknown names warn with the registered vocabulary. |

## Slots

| Slot | Description | Notes |
|---|---|---|
| *(default)* | The label content (the accessible name via the wrapping label). | required |
| `checked` | Indicator content shown while selected; replaces the drawn dot. Any markup: svg, icon-font glyph, HTML character, image. | aria-hidden; sized by `indicator-size`; colored by `indicator-ink` |
| `unchecked` | Indicator content shown while not selected. | aria-hidden; sized by `indicator-size`; colored by `indicator-ink` |

- `checked`: The control is aria-hidden — the state is conveyed by the native input, so indicator content is purely visual.

## Keyboard

- `ArrowDown / ArrowRight, ArrowUp / ArrowLeft` — Moves the group's selection to the next / previous radio (native behavior, preserved).
- `Space` — Selects the focused radio (native).
- `Tab` — The group is a single tab stop; focus lands on the selected radio (native).

## Options (behavior)

Named, defaulted decisions about what this component conveniently *does*, set under `components.radio.options` in zebkit.config.json and carried into the browser by the generated runtime config module. `false` is the off switch for any of them. Precedence is one chain with no exceptions: **instance attribute > project config > zebkit default** — whatever the config decided, the markup outranks it.

| Option | Type | Default | Does |
|---|---|---|---|
| `discoverToggledElementTimeout` | `number` | `2000` | How long to watch for a visibility target that mounts after its trigger, in milliseconds; false or an instance value of 0 diagnoses immediately. |

## Composites

Visibility participant: **trigger** (binding). Author one of `toggles="{id ...}"`, `shows="{id ...}"`, `hides="{id ...}"`. It may emit `aria-controls` according to the target mechanism. See [Visibility composites](composites.md) for target resolution, events, late mounting, and degradation.

## Events

No custom events. Native events (`click`, `change`, `input`, ...) bubble from the internal native element in light DOM — listen on the zebkit element.

## Examples

**Basic group**

```html
<zbk-radio name="plan" value="monthly" checked>Monthly</zbk-radio><zbk-radio name="plan" value="annual">Annual</zbk-radio>
```

The shared name is what makes them one control; checked marks the group's current selection.

**Custom indicator**

```html
<zbk-radio name="mood" value="good"><span slot="checked">●</span>Good</zbk-radio>
```

Indicator content layers over the aria-hidden control, sized by indicator-size and colored by indicator-ink.

**Bound panels**

```html
<zbk-radio name="view" value="summary" toggles="summary-panel" checked>Summary</zbk-radio><zbk-radio name="view" value="details" toggles="details-panel">Details</zbk-radio><section id="summary-panel">Summary content.</section><section id="details-panel" hidden="until-found">Detailed content.</section>
```

Use toggles on every radio: native group exclusivity hides the previous panel when its radio leaves checked.

**Lone radio** (discouraged)

```html
<zbk-radio name="agree">I agree</zbk-radio>
```

A radio without group siblings can never be unchecked once selected — an on/off choice is zbk-checkbox.

## Tokens (CSS custom properties)

Values are alias references (`{family.name}` compiles to `var(--zbk-family-name)`) or structural literals. Override any of them on a selector to re-style; nothing else is needed.

| Token | Default | Type | Condition | Description |
|---|---|---|---|---|
| `--zbk-radio-control-width` | `{spacing.105}` | dimension | — | Width of the radio control. |
| `--zbk-radio-control-height` | `{spacing.105}` | dimension | — | Height of the radio control. |
| `--zbk-radio-canvas` | `{app.canvas}` | color | — | Background of an unselected radio control. |
| `--zbk-radio-canvas-hover` | `{app.canvas-subtle}` | color | — | Control background when hovered. |
| `--zbk-radio-canvas-active` | `{app.canvas-muted}` | color | — | Control background while pressed. |
| `--zbk-radio-canvas-checked` | `{color.global-black}` | color | — | Control background when selected. |
| `--zbk-radio-canvas-checked-hover` | `{radio.canvas-checked}` | color | — | Control background when selected and hovered. |
| `--zbk-radio-canvas-checked-active` | `{radio.canvas-checked}` | color | — | Control background when selected and pressed. |
| `--zbk-radio-canvas-checked-disabled` | `{radio.canvas-disabled}` | color | — | Control background when selected and disabled. |
| `--zbk-radio-canvas-disabled` | `{disabled.canvas}` | color | — | Control background when disabled. |
| `--zbk-radio-border-color` | `{app.border-emphasis}` | color | — | Border color of an unselected radio control. |
| `--zbk-radio-border-color-hover` | `{action.ink}` | color | — | Control border color when hovered. |
| `--zbk-radio-border-color-active` | `{action.ink}` | color | — | Control border color while pressed. |
| `--zbk-radio-border-color-checked` | `{action.ink}` | color | — | Control border color when selected. |
| `--zbk-radio-border-color-checked-hover` | `{radio.border-color-checked}` | color | — | Control border color when selected and hovered. |
| `--zbk-radio-border-color-checked-active` | `{radio.border-color-checked}` | color | — | Control border color when selected and pressed. |
| `--zbk-radio-border-color-checked-disabled` | `{radio.border-color-disabled}` | color | — | Control border color when selected and disabled. |
| `--zbk-radio-border-color-disabled` | `{disabled.border}` | color | — | Control border color when disabled. |
| `--zbk-radio-border-width` | `{border.width-md}` | dimension | — | Border thickness of the radio control. |
| `--zbk-radio-border-style` | `{border.style}` | strokeStyle | — | Border style of the radio control. |
| `--zbk-radio-border-radius` | `50%` | cssDimension | — | Corner radius of the radio control (a circle by default). |
| `--zbk-radio-indicator-canvas` | `{action.ink}` | color | — | Indicator surface background when unselected. |
| `--zbk-radio-indicator-canvas-hover` | `{radio.indicator-canvas}` | color | — | Indicator surface background when hovered. |
| `--zbk-radio-indicator-canvas-active` | `{radio.indicator-canvas}` | color | — | Indicator surface background while pressed. |
| `--zbk-radio-indicator-canvas-checked` | `{radio.indicator-canvas}` | color | — | Indicator surface background when selected. |
| `--zbk-radio-indicator-canvas-checked-hover` | `{radio.indicator-canvas-checked}` | color | — | Indicator surface background when selected and hovered. |
| `--zbk-radio-indicator-canvas-checked-active` | `{radio.indicator-canvas-checked}` | color | — | Indicator surface background when selected and pressed. |
| `--zbk-radio-indicator-canvas-checked-disabled` | `{radio.indicator-canvas-disabled}` | color | — | Indicator surface background when selected and disabled. |
| `--zbk-radio-indicator-canvas-disabled` | `{disabled.ink}` | color | — | Indicator surface background when disabled. |
| `--zbk-radio-indicator-ink` | `{action.ink}` | color | — | Slotted indicator content color when unselected. |
| `--zbk-radio-indicator-ink-hover` | `{radio.indicator-ink}` | color | — | Slotted indicator content color when hovered. |
| `--zbk-radio-indicator-ink-active` | `{radio.indicator-ink}` | color | — | Slotted indicator content color while pressed. |
| `--zbk-radio-indicator-ink-checked` | `{radio.indicator-ink}` | color | — | Slotted indicator content color when selected. |
| `--zbk-radio-indicator-ink-checked-hover` | `{radio.indicator-ink-checked}` | color | — | Slotted indicator content color when selected and hovered. |
| `--zbk-radio-indicator-ink-checked-active` | `{radio.indicator-ink-checked}` | color | — | Slotted indicator content color when selected and pressed. |
| `--zbk-radio-indicator-ink-checked-disabled` | `{radio.indicator-ink-disabled}` | color | — | Slotted indicator content color when selected and disabled. |
| `--zbk-radio-indicator-ink-disabled` | `{disabled.ink}` | color | — | Slotted indicator content color when disabled. |
| `--zbk-radio-indicator-border-color` | `{radio.indicator-canvas}` | color | — | Indicator border color when unselected. |
| `--zbk-radio-indicator-border-color-hover` | `{radio.indicator-border-color}` | color | — | Indicator border color when hovered. |
| `--zbk-radio-indicator-border-color-active` | `{radio.indicator-border-color}` | color | — | Indicator border color while pressed. |
| `--zbk-radio-indicator-border-color-checked` | `{radio.indicator-border-color}` | color | — | Indicator border color when selected. |
| `--zbk-radio-indicator-border-color-checked-hover` | `{radio.indicator-border-color-checked}` | color | — | Indicator border color when selected and hovered. |
| `--zbk-radio-indicator-border-color-checked-active` | `{radio.indicator-border-color-checked}` | color | — | Indicator border color when selected and pressed. |
| `--zbk-radio-indicator-border-color-checked-disabled` | `{radio.indicator-border-color-disabled}` | color | — | Indicator border color when selected and disabled. |
| `--zbk-radio-indicator-border-color-disabled` | `{radio.indicator-border-color}` | color | — | Indicator border color when disabled. |
| `--zbk-radio-indicator-border-width` | `{border.width-sm}` | dimension | — | Indicator border thickness; zero preserves the standard dot. |
| `--zbk-radio-indicator-border-style` | `{border.style}` | strokeStyle | — | Indicator border line style. |
| `--zbk-radio-indicator-size` | `{spacing.05}` | cssDimension | — | Diameter of the selection dot and size of slotted indicator content; em-based so size variants rescale it through font-size. |
| `--zbk-radio-indicator-radius` | `50%` | cssDimension | — | Corner radius of the selection dot (a circle by default). |
| `--zbk-radio-indicator-box-shadow` | `0 0 0 var(--zbk-spacing-025) var(--zbk-color-global-black)` | shadow | — | Default indicator shadow. |
| `--zbk-radio-indicator-box-shadow-hover` | `{radio.indicator-box-shadow}` | shadow | — | Indicator shadow when hovered. |
| `--zbk-radio-indicator-box-shadow-active` | `{radio.indicator-box-shadow}` | shadow | — | Indicator shadow while pressed. |
| `--zbk-radio-indicator-box-shadow-checked` | `{radio.indicator-box-shadow}` | shadow | — | Indicator shadow when selected. |
| `--zbk-radio-indicator-box-shadow-checked-hover` | `{radio.indicator-box-shadow-checked}` | shadow | — | Indicator shadow when selected and hovered. |
| `--zbk-radio-indicator-box-shadow-checked-active` | `{radio.indicator-box-shadow-checked}` | shadow | — | Indicator shadow when selected and pressed. |
| `--zbk-radio-indicator-box-shadow-checked-disabled` | `{radio.indicator-box-shadow-disabled}` | shadow | — | Indicator shadow when selected and disabled. |
| `--zbk-radio-indicator-box-shadow-focus` | `{radio.indicator-box-shadow}` | shadow | — | Indicator shadow when the native radio has keyboard focus. |
| `--zbk-radio-indicator-box-shadow-disabled` | `none` | shadow | — | Indicator shadow when disabled. |
| `--zbk-radio-indicator-transform` | `scale(0)` | transform | — | Indicator transform when unselected; scale(0) hides the standard dot. |
| `--zbk-radio-indicator-transform-hover` | `{radio.indicator-transform}` | transform | — | Indicator transform when an unselected radio is hovered. |
| `--zbk-radio-indicator-transform-active` | `{radio.indicator-transform}` | transform | — | Indicator transform while an unselected radio is pressed. |
| `--zbk-radio-indicator-transform-checked` | `scale(1)` | transform | — | Indicator transform when selected; scale(1) reveals the standard dot. |
| `--zbk-radio-indicator-transform-checked-hover` | `{radio.indicator-transform-checked}` | transform | — | Indicator transform when selected and hovered. |
| `--zbk-radio-indicator-transform-checked-active` | `{radio.indicator-transform-checked}` | transform | — | Indicator transform when selected and pressed. |
| `--zbk-radio-indicator-transform-checked-disabled` | `{radio.indicator-transform-checked}` | transform | — | Indicator transform when selected and disabled. |
| `--zbk-radio-indicator-transform-focus` | `{radio.indicator-transform}` | transform | — | Indicator transform when an unselected radio has keyboard focus. |
| `--zbk-radio-indicator-transform-disabled` | `{radio.indicator-transform}` | transform | — | Indicator transform when an unselected radio is disabled. |
| `--zbk-radio-indicator-transition-duration` | `{radio.transition-duration}` | duration | — | Duration for indicator surface state transitions. **(a11y)** |
| `--zbk-radio-indicator-transition-timing-function` | `{radio.transition-timing-function}` | cubicBezier | — | Easing for indicator surface state transitions. |
| `--zbk-radio-indicator-transition-property` | `background-color, border-color, box-shadow, transform, opacity` | transitionProperty | — | Indicator surface properties that animate on state changes. |
| `--zbk-radio-indicator-transition-delay` | `{radio.transition-delay}` | duration | — | Delay before indicator surface transitions run. |
| `--zbk-radio-ink` | `{app.ink}` | color | — | Label text color. |
| `--zbk-radio-ink-disabled` | `{disabled.ink}` | color | — | Label text color when disabled. |
| `--zbk-radio-font-family` | `{font-family.interface}` | fontFamily | — | Font family for the radio label. |
| `--zbk-radio-font-size` | `{font-size.md}` | cssDimension | — | Font size for the radio label. |
| `--zbk-radio-font-weight` | `{font-weight.normal}` | fontWeight | — | Font weight for the radio label. |
| `--zbk-radio-line-height` | `{line-height.2}` | number | — | Line height for the radio label. |
| `--zbk-radio-letter-spacing` | `{letter-spacing.normal}` | cssDimension | — | Letter spacing for the radio label. |
| `--zbk-radio-gap` | `{spacing.sm}` | dimension | — | Space between the control and its label. |
| `--zbk-radio-align-items` | `center` | flex | — | Cross-axis alignment of control and label. |
| `--zbk-radio-group-gap` | `{spacing.md}` | dimension | — | Gap between radios in a .zbk-radio-group. |
| `--zbk-radio-group-direction` | `column` | flex | — | Flow direction of a .zbk-radio-group (column or row). |
| `--zbk-radio-focus-color` | `{focus.color}` | color | — | Outline color for keyboard focus. |
| `--zbk-radio-focus-width` | `{focus.width}` | dimension | — | Outline width for keyboard focus. |
| `--zbk-radio-focus-offset` | `{focus.offset}` | dimension | — | Outline offset for keyboard focus. |
| `--zbk-radio-box-shadow` | `none` | shadow | — | Default control shadow. |
| `--zbk-radio-box-shadow-hover` | `none` | shadow | — | Control shadow when hovered. |
| `--zbk-radio-box-shadow-active` | `none` | shadow | — | Control shadow while pressed. |
| `--zbk-radio-box-shadow-checked` | `none` | shadow | — | Control shadow when selected. |
| `--zbk-radio-box-shadow-checked-hover` | `{radio.box-shadow-checked}` | shadow | — | Control shadow when selected and hovered. |
| `--zbk-radio-box-shadow-checked-active` | `{radio.box-shadow-checked}` | shadow | — | Control shadow when selected and pressed. |
| `--zbk-radio-box-shadow-checked-disabled` | `none` | shadow | — | Control shadow when selected and disabled. |
| `--zbk-radio-box-shadow-focus` | `none` | shadow | — | Control shadow in the focus state (in addition to the outline). |
| `--zbk-radio-box-shadow-disabled` | `none` | shadow | — | Control shadow when disabled. |
| `--zbk-radio-cursor` | `pointer` | cursor | — | Cursor when hovering an enabled radio. |
| `--zbk-radio-cursor-disabled` | `not-allowed` | cursor | — | Cursor when hovering a disabled radio. |
| `--zbk-radio-transition-duration` | `{transition.playful-fx-duration-default}` | duration | — | Duration for radio state transitions. **(a11y)** |
| `--zbk-radio-transition-timing-function` | `{transition.playful-fx-function-default}` | cubicBezier | — | Easing for radio state transitions. |
| `--zbk-radio-transition-property` | `background-color, border-color, box-shadow, transform, opacity, outline` | transitionProperty | — | CSS properties that animate on state changes. |
| `--zbk-radio-transition-delay` | `0ms` | duration | — | Delay before radio transitions run. |
| `--zbk-radio-opacity` | `1` | number | — | Opacity of the radio. |
| `--zbk-radio-opacity-disabled` | `{opacity.70}` | number | — | Control opacity when disabled. |

## Variants

A variant is a named, partial remapping of the token surface compiled to a class. Apply via `variant="name other-name"`; different axes compose, same-axis variants are alternatives.

| Variant | Axis | Class | Description | Overrides |
|---|---|---|---|---|
| `lg` | size | `zbk-radio--lg` | Larger control and label type. | control-width: {spacing.lg}; control-height: {spacing.lg}; font-size: {font-size.lg} |
| `sm` | size | `zbk-radio--sm` | Smaller control and label type. | control-width: {spacing.md}; control-height: {spacing.md}; font-size: {font-size.sm} |

Custom variants: add a `zbk-radio.variants.json` file to the base theme's token folder (component-keyed map of `{ "radio": { "{name}": { "overrides": { ... } } } }`; token keys must exist in the table above, values are alias references or structural literals). A shipped variant name patches that variant's CSS — usable immediately. A new name compiles a new `.zbk-radio--{name}` class and additionally needs `ZebkitElement.registerVariants(json)` before elements upgrade so `variant="{name}"` validates and applies it.

Related: `<zbk-checkbox>`, `<zbk-toggle>`, `<zbk-select>`.

## Resolved options in this project

What `components.radio.options` resolves to here, carried into the browser by the generated runtime config module. An instance attribute always outranks these (GRAMMAR.md §11).

| Option | Value |
|---|---|
| `discoverToggledElementTimeout` | `2000` |
