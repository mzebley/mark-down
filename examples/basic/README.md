# mark↓ Example

This placeholder folder illustrates how you might wire mark↓ into an application. Point the CLI at your snippet directory:

```
mark-down build content/snippets
```

Then consume the manifest via the runtime clients.
